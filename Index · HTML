<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>KEY MOVIES</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Nunito:wght@300;400;600;700;800&display=swap" rel="stylesheet">
<style>
/* ── RESET & VARIABLES ──────────────────────────────────────── */
:root{
  --bg:#0a0a0f;--bg2:#12121a;--bg3:#1a1a28;--card:#15151f;
  --gold:#f5c518;--red:#e50914;--blue:#2d9cdb;
  --txt:#e8e8f0;--txt2:#9999b0;--txt3:#55556a;
  --radius:8px;--gap:16px;
  --nav-h:64px;
}
*{margin:0;padding:0;box-sizing:border-box}
html{scroll-behavior:smooth}
body{background:var(--bg);color:var(--txt);font-family:'Nunito',sans-serif;min-height:100vh;overflow-x:hidden}
a{color:inherit;text-decoration:none}
img{display:block;max-width:100%}
button{cursor:pointer;border:none;background:none;font-family:inherit}
input,select,textarea{font-family:inherit}
::-webkit-scrollbar{width:5px;height:5px}
::-webkit-scrollbar-track{background:var(--bg2)}
::-webkit-scrollbar-thumb{background:var(--bg3);border-radius:3px}
::-webkit-scrollbar-thumb:hover{background:#444}

/* ── NAVBAR ─────────────────────────────────────────────────── */
#navbar{
  position:fixed;top:0;left:0;right:0;z-index:900;
  height:var(--nav-h);
  background:linear-gradient(to bottom,rgba(10,10,15,.98),transparent);
  display:flex;align-items:center;padding:0 24px;gap:24px;
  transition:background .3s;
}
#navbar.scrolled{background:rgba(10,10,15,.98);box-shadow:0 2px 20px rgba(0,0,0,.5)}
.logo{display:flex;align-items:center;gap:8px;flex-shrink:0}
.logo-k{background:var(--gold);color:#000;font-family:'Bebas Neue';font-size:22px;width:36px;height:36px;display:grid;place-items:center;border-radius:6px;font-weight:900}
.logo-text{font-family:'Bebas Neue';font-size:22px;letter-spacing:2px;color:var(--gold)}
.nav-links{display:flex;gap:4px;align-items:center}
.nav-links a{padding:6px 12px;border-radius:var(--radius);font-size:.88rem;font-weight:600;color:var(--txt2);transition:all .2s}
.nav-links a:hover,.nav-links a.active{color:var(--txt);background:rgba(255,255,255,.07)}
.nav-right{margin-left:auto;display:flex;align-items:center;gap:10px}
#searchBar{position:relative}
#searchBar input{background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:20px;padding:7px 16px 7px 38px;color:var(--txt);font-size:.85rem;width:200px;transition:all .3s;outline:none}
#searchBar input:focus{width:260px;border-color:var(--gold);background:rgba(255,255,255,.12)}
#searchBar .s-icon{position:absolute;left:12px;top:50%;transform:translateY(-50%);opacity:.5;pointer-events:none}
.btn{padding:8px 18px;border-radius:var(--radius);font-weight:700;font-size:.85rem;transition:all .2s;cursor:pointer}
.btn-gold{background:var(--gold);color:#000}
.btn-gold:hover{background:#e6b800}
.btn-outline{border:1.5px solid rgba(255,255,255,.3);color:var(--txt2)}
.btn-outline:hover{border-color:var(--txt);color:var(--txt);background:rgba(255,255,255,.06)}
.btn-red{background:var(--red);color:#fff}
.btn-red:hover{background:#c8000f}
.btn-sm{padding:5px 12px;font-size:.78rem}
#userMenu{position:relative}
.user-avatar{width:36px;height:36px;border-radius:50%;background:var(--gold);display:grid;place-items:center;font-weight:800;font-size:.85rem;color:#000;cursor:pointer;border:2px solid transparent;transition:border-color .2s}
.user-avatar:hover{border-color:var(--gold)}
.user-dropdown{position:absolute;top:44px;right:0;background:var(--bg3);border:1px solid rgba(255,255,255,.1);border-radius:var(--radius);min-width:170px;padding:6px 0;display:none;z-index:9999;box-shadow:0 8px 30px rgba(0,0,0,.5)}
.user-dropdown.open{display:block}
.user-dropdown a,.user-dropdown button{display:flex;align-items:center;gap:10px;padding:9px 16px;width:100%;font-size:.85rem;color:var(--txt2);transition:all .2s;text-align:left}
.user-dropdown a:hover,.user-dropdown button:hover{background:rgba(255,255,255,.06);color:var(--txt)}
.lang-btn{background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:20px;padding:5px 12px;font-size:.78rem;color:var(--txt2);font-weight:700;letter-spacing:1px}
.lang-btn:hover{border-color:var(--gold);color:var(--gold)}

/* ── HERO ────────────────────────────────────────────────────── */
#hero{
  position:relative;height:100vh;min-height:560px;
  display:flex;align-items:flex-end;padding-bottom:80px;
  overflow:hidden;background:#000;
}
.hero-bg{position:absolute;inset:0;background-size:cover;background-position:center top;transition:opacity 1s}
.hero-bg::after{content:'';position:absolute;inset:0;background:linear-gradient(to right,rgba(10,10,15,.95) 30%,rgba(10,10,15,.4) 70%,rgba(10,10,15,.1));background:linear-gradient(135deg,rgba(10,10,15,1) 0%,rgba(10,10,15,.85) 40%,rgba(10,10,15,.2) 100%)}
.hero-content{position:relative;z-index:2;padding:0 60px;max-width:620px}
.hero-badge{display:inline-flex;align-items:center;gap:6px;background:var(--red);border-radius:4px;padding:3px 10px;font-size:.72rem;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;margin-bottom:16px}
.hero-title{font-family:'Bebas Neue';font-size:clamp(3rem,7vw,6rem);line-height:1;letter-spacing:2px;margin-bottom:12px;text-shadow:0 2px 20px rgba(0,0,0,.8)}
.hero-meta{display:flex;align-items:center;gap:12px;font-size:.85rem;color:var(--txt2);margin-bottom:14px}
.hero-meta .rating{color:var(--gold);font-weight:800}
.hero-desc{font-size:.93rem;color:rgba(232,232,240,.75);line-height:1.6;margin-bottom:24px;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}
.hero-btns{display:flex;gap:12px;flex-wrap:wrap}
.btn-play{display:flex;align-items:center;gap:8px;padding:12px 28px;background:var(--red);color:#fff;border-radius:var(--radius);font-weight:700;font-size:.95rem;transition:all .2s}
.btn-play:hover{background:#c8000f;transform:scale(1.04)}
.btn-watch{display:flex;align-items:center;gap:8px;padding:12px 24px;background:rgba(255,255,255,.15);color:#fff;border-radius:var(--radius);font-weight:700;font-size:.95rem;backdrop-filter:blur(4px);transition:all .2s}
.btn-watch:hover{background:rgba(255,255,255,.25)}
.hero-dots{position:absolute;bottom:30px;left:60px;display:flex;gap:8px}
.hero-dot{width:28px;height:4px;border-radius:2px;background:rgba(255,255,255,.3);cursor:pointer;transition:all .3s}
.hero-dot.active{background:var(--gold);width:44px}
.hero-arrows{position:absolute;right:40px;bottom:50%;transform:translateY(50%);display:flex;flex-direction:column;gap:10px}
.hero-arrow{width:40px;height:40px;border-radius:50%;background:rgba(255,255,255,.1);backdrop-filter:blur(4px);display:grid;place-items:center;cursor:pointer;transition:all .2s;border:1px solid rgba(255,255,255,.15)}
.hero-arrow:hover{background:rgba(255,255,255,.2)}

/* ── MAIN CONTENT ────────────────────────────────────────────── */
#main{padding-top:30px}
.section{padding:30px 40px;overflow:hidden}
.section-header{display:flex;align-items:center;gap:16px;margin-bottom:20px}
.section-title{font-family:'Bebas Neue';font-size:1.5rem;letter-spacing:1px;color:var(--txt)}
.section-title span{color:var(--gold)}
.section-line{flex:1;height:1px;background:linear-gradient(to right,rgba(245,197,24,.2),transparent)}
.section-nav{display:flex;gap:6px}
.section-nav button{width:30px;height:30px;border-radius:50%;background:var(--bg3);display:grid;place-items:center;font-size:1.1rem;color:var(--txt2);transition:all .2s}
.section-nav button:hover{background:var(--gold);color:#000}

/* ── MOVIE CARDS ─────────────────────────────────────────────── */
.movies-row{display:flex;gap:14px;overflow-x:auto;padding-bottom:10px;scroll-snap-type:x mandatory}
.movies-row::-webkit-scrollbar{height:3px}
.movies-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:16px}
.movie-card{flex-shrink:0;width:160px;scroll-snap-align:start;cursor:pointer;transition:transform .25s;position:relative}
.movies-grid .movie-card{width:100%}
.movie-card:hover{transform:scale(1.05) translateY(-4px)}
.movie-card:hover .card-overlay{opacity:1}
.card-thumb{border-radius:var(--radius);overflow:hidden;aspect-ratio:2/3;background:var(--bg3);position:relative}
.card-thumb img{width:100%;height:100%;object-fit:cover;transition:transform .3s}
.movie-card:hover .card-thumb img{transform:scale(1.08)}
.card-overlay{position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,.9),transparent 50%);opacity:0;transition:opacity .3s;border-radius:var(--radius);display:flex;flex-direction:column;justify-content:flex-end;padding:12px}
.card-overlay .play-btn{width:40px;height:40px;border-radius:50%;background:var(--red);display:grid;place-items:center;margin:0 auto 10px;transition:transform .2s}
.card-overlay .play-btn:hover{transform:scale(1.15)}
.card-badges{position:absolute;top:8px;left:8px;display:flex;gap:4px;flex-wrap:wrap}
.badge{padding:2px 7px;border-radius:3px;font-size:.65rem;font-weight:800;letter-spacing:.5px;text-transform:uppercase}
.badge-new{background:var(--red);color:#fff}
.badge-cat{background:rgba(245,197,24,.2);color:var(--gold);border:1px solid rgba(245,197,24,.3)}
.badge-ep{background:rgba(45,156,219,.2);color:var(--blue);border:1px solid rgba(45,156,219,.3)}
.card-info{margin-top:8px}
.card-title{font-size:.82rem;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.card-meta{font-size:.72rem;color:var(--txt2);margin-top:2px;display:flex;align-items:center;gap:6px}
.card-rating{color:var(--gold);font-weight:800}

/* ── CATEGORY FILTER ─────────────────────────────────────────── */
.cat-filters{display:flex;gap:8px;overflow-x:auto;padding:0 40px 20px;scrollbar-width:none}
.cat-filters::-webkit-scrollbar{display:none}
.cat-btn{padding:7px 18px;border-radius:20px;font-size:.8rem;font-weight:700;border:1.5px solid rgba(255,255,255,.1);color:var(--txt2);transition:all .2s;white-space:nowrap}
.cat-btn:hover{border-color:var(--gold);color:var(--gold)}
.cat-btn.active{background:var(--gold);color:#000;border-color:var(--gold)}

/* ── SEARCH RESULTS ──────────────────────────────────────────── */
#searchResults{
  position:fixed;top:var(--nav-h);left:0;right:0;bottom:0;z-index:800;
  background:rgba(10,10,15,.97);backdrop-filter:blur(12px);
  overflow-y:auto;padding:30px 40px;display:none;
}
#searchResults.open{display:block}
.sr-title{font-family:'Bebas Neue';font-size:1.8rem;margin-bottom:8px}
.sr-query{color:var(--gold)}
.sr-sub{color:var(--txt2);font-size:.85rem;margin-bottom:24px}
.no-results-box{background:var(--bg3);border-radius:12px;padding:40px;text-align:center;margin-top:20px}
.no-results-box h3{font-size:1.1rem;margin-bottom:8px}
.no-results-box p{color:var(--txt2);font-size:.88rem}

/* ── MODAL BASE ──────────────────────────────────────────────── */
.modal-overlay{
  position:fixed;inset:0;z-index:1000;
  background:rgba(0,0,0,.85);backdrop-filter:blur(8px);
  display:grid;place-items:center;padding:20px;
  opacity:0;pointer-events:none;transition:opacity .3s;
}
.modal-overlay.open{opacity:1;pointer-events:all}
.modal{background:var(--bg2);border:1px solid rgba(255,255,255,.08);border-radius:16px;width:100%;max-height:90vh;overflow-y:auto;position:relative;transform:scale(.96);transition:transform .3s}
.modal-overlay.open .modal{transform:scale(1)}
.modal-close{position:absolute;top:16px;right:16px;width:32px;height:32px;border-radius:50%;background:rgba(255,255,255,.08);display:grid;place-items:center;cursor:pointer;font-size:1.1rem;z-index:10;transition:background .2s}
.modal-close:hover{background:rgba(255,255,255,.18)}

/* ── AUTH MODAL ──────────────────────────────────────────────── */
#authModal .modal{max-width:420px}
.auth-logo{text-align:center;padding:32px 24px 0}
.auth-logo .logo-k{margin:0 auto 10px;width:50px;height:50px;font-size:28px;border-radius:10px}
.auth-tabs{display:flex;gap:0;border-bottom:1px solid rgba(255,255,255,.08);margin:20px 24px 0}
.auth-tab{flex:1;padding:10px;text-align:center;font-size:.85rem;font-weight:700;color:var(--txt2);border-bottom:2px solid transparent;cursor:pointer;transition:all .2s}
.auth-tab.active{color:var(--gold);border-color:var(--gold)}
.auth-body{padding:24px}
.form-group{margin-bottom:16px}
.form-label{font-size:.78rem;font-weight:700;color:var(--txt2);margin-bottom:6px;display:block;text-transform:uppercase;letter-spacing:.5px}
.form-input{width:100%;background:rgba(255,255,255,.06);border:1.5px solid rgba(255,255,255,.1);border-radius:var(--radius);padding:10px 14px;color:var(--txt);font-size:.88rem;outline:none;transition:all .2s}
.form-input:focus{border-color:var(--gold);background:rgba(255,255,255,.09)}
.form-select{width:100%;background:rgba(255,255,255,.06);border:1.5px solid rgba(255,255,255,.1);border-radius:var(--radius);padding:10px 14px;color:var(--txt);font-size:.88rem;outline:none;appearance:none;cursor:pointer}
.form-select option{background:var(--bg2);color:var(--txt)}
.form-footer{text-align:center;font-size:.8rem;color:var(--txt2);margin-top:12px}
.form-footer a{color:var(--gold)}

/* ── MOVIE DETAIL MODAL ───────────────────────────────────────── */
#movieModal .modal{max-width:900px;background:var(--bg)}
.movie-modal-top{position:relative;height:420px;overflow:hidden}
.movie-modal-bg{width:100%;height:100%;object-fit:cover}
.movie-modal-gradient{position:absolute;inset:0;background:linear-gradient(to top,var(--bg) 20%,rgba(10,10,15,.4) 100%)}
.movie-modal-info{position:absolute;bottom:0;left:0;right:0;padding:28px 32px}
.movie-modal-badge{display:inline-flex;gap:6px;margin-bottom:10px}
.movie-modal-title{font-family:'Bebas Neue';font-size:2.8rem;line-height:1;margin-bottom:8px}
.movie-modal-meta{display:flex;gap:14px;font-size:.82rem;color:var(--txt2);align-items:center;flex-wrap:wrap}
.movie-modal-body{padding:0 32px 32px}
.movie-modal-actions{display:flex;gap:10px;margin:20px 0;flex-wrap:wrap}
.movie-modal-actions .btn{display:flex;align-items:center;gap:8px;padding:10px 22px;font-size:.88rem}
.movie-modal-desc{color:rgba(232,232,240,.75);line-height:1.7;font-size:.9rem;margin-bottom:24px}
/* Video Player */
#videoPlayer{margin:0 32px 24px;border-radius:12px;overflow:hidden;background:#000;aspect-ratio:16/9;display:none}
#videoPlayer iframe,#videoPlayer video{width:100%;height:100%;border:none}
/* Comments */
.comments-section{border-top:1px solid rgba(255,255,255,.06);padding-top:24px}
.comments-section h3{font-size:1rem;font-weight:700;margin-bottom:16px;display:flex;align-items:center;gap:8px}
.comment-form{display:flex;gap:10px;margin-bottom:20px}
.comment-form textarea{flex:1;background:rgba(255,255,255,.06);border:1.5px solid rgba(255,255,255,.1);border-radius:var(--radius);padding:10px 14px;color:var(--txt);font-size:.85rem;resize:none;height:60px;outline:none;transition:border-color .2s}
.comment-form textarea:focus{border-color:var(--gold)}
.comment-item{padding:12px 0;border-bottom:1px solid rgba(255,255,255,.04);display:flex;gap:12px}
.comment-avatar{width:32px;height:32px;border-radius:50%;background:var(--gold);display:grid;place-items:center;font-size:.75rem;font-weight:800;color:#000;flex-shrink:0}
.comment-right .comment-author{font-size:.8rem;font-weight:700;margin-bottom:3px}
.comment-right .comment-text{font-size:.82rem;color:rgba(232,232,240,.75);line-height:1.5}
.comment-right .comment-date{font-size:.7rem;color:var(--txt3);margin-top:4px}
.login-to-comment{background:rgba(255,255,255,.04);border:1px dashed rgba(255,255,255,.1);border-radius:var(--radius);padding:16px;text-align:center;font-size:.85rem;color:var(--txt2);cursor:pointer}
.login-to-comment:hover{border-color:var(--gold);color:var(--gold)}

/* ── ADMIN PANEL ─────────────────────────────────────────────── */
#adminPanel{
  position:fixed;inset:0;z-index:2000;
  background:var(--bg);display:none;overflow:hidden;
  flex-direction:column;
}
#adminPanel.open{display:flex}
.admin-bar{height:56px;background:var(--bg2);border-bottom:1px solid rgba(255,255,255,.07);display:flex;align-items:center;padding:0 24px;gap:16px}
.admin-title{font-family:'Bebas Neue';font-size:1.4rem;letter-spacing:1px;color:var(--gold)}
.admin-nav{display:flex;gap:2px;overflow-x:auto}
.admin-nav button{padding:6px 16px;border-radius:6px;font-size:.8rem;font-weight:700;color:var(--txt2);transition:all .2s;white-space:nowrap}
.admin-nav button.active,.admin-nav button:hover{background:rgba(245,197,24,.12);color:var(--gold)}
.admin-close{margin-left:auto;width:32px;height:32px;border-radius:50%;background:rgba(255,255,255,.07);display:grid;place-items:center;cursor:pointer}
.admin-body{flex:1;overflow-y:auto;padding:24px}
/* Stats cards */
.stats-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:16px;margin-bottom:32px}
.stat-card{background:var(--bg3);border-radius:12px;padding:20px;text-align:center;border:1px solid rgba(255,255,255,.06)}
.stat-icon{font-size:1.8rem;margin-bottom:8px}
.stat-num{font-family:'Bebas Neue';font-size:2.5rem;color:var(--gold);line-height:1}
.stat-label{font-size:.75rem;color:var(--txt2);text-transform:uppercase;letter-spacing:.5px;margin-top:4px}
/* Admin tables */
.admin-table-wrap{background:var(--bg3);border-radius:12px;overflow:hidden;border:1px solid rgba(255,255,255,.06)}
.admin-table-head{display:flex;align-items:center;padding:16px 20px;border-bottom:1px solid rgba(255,255,255,.06);gap:12px}
.admin-table-head h3{font-size:.95rem;font-weight:800;flex:1}
table.data-table{width:100%;border-collapse:collapse}
table.data-table th{padding:10px 14px;font-size:.72rem;font-weight:800;color:var(--txt2);text-transform:uppercase;letter-spacing:.5px;text-align:left;border-bottom:1px solid rgba(255,255,255,.06)}
table.data-table td{padding:10px 14px;font-size:.82rem;color:var(--txt2);border-bottom:1px solid rgba(255,255,255,.04);vertical-align:middle}
table.data-table tr:last-child td{border-bottom:none}
table.data-table tr:hover td{background:rgba(255,255,255,.03);color:var(--txt)}
.status-active{color:#27ae60;font-weight:700}
.status-blocked{color:var(--red);font-weight:700}
/* Movie form */
.movie-form{background:var(--bg3);border-radius:12px;padding:24px;border:1px solid rgba(255,255,255,.06);margin-bottom:24px}
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.form-grid .full{grid-column:1/-1}
/* Traffic */
.traffic-item{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,.04);font-size:.8rem}
.traffic-time{color:var(--txt3);min-width:140px;font-size:.72rem}
.traffic-user{color:var(--gold);font-weight:700}
.traffic-action{background:rgba(255,255,255,.06);border-radius:4px;padding:2px 8px;font-size:.7rem}
.traffic-country{color:var(--txt2)}

/* ── TOAST ───────────────────────────────────────────────────── */
#toast{
  position:fixed;bottom:30px;left:50%;transform:translateX(-50%) translateY(20px);
  background:var(--bg3);border:1px solid rgba(255,255,255,.1);border-radius:10px;
  padding:12px 24px;font-size:.88rem;font-weight:600;
  opacity:0;transition:all .3s;z-index:9999;pointer-events:none;
  box-shadow:0 8px 30px rgba(0,0,0,.5);
}
#toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
#toast.success{border-color:rgba(39,174,96,.4);color:#27ae60}
#toast.error{border-color:rgba(229,9,20,.4);color:var(--red)}

/* ── LOADING ─────────────────────────────────────────────────── */
.spinner{width:28px;height:28px;border:3px solid rgba(255,255,255,.1);border-top-color:var(--gold);border-radius:50%;animation:spin .8s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
#pageLoader{position:fixed;inset:0;background:var(--bg);z-index:9998;display:grid;place-items:center}
.loader-logo{text-align:center}
.loader-logo .logo-k{width:60px;height:60px;font-size:32px;margin:0 auto 16px;border-radius:12px;animation:pulse 1.5s ease infinite}
@keyframes pulse{0%,100%{transform:scale(1);box-shadow:0 0 0 0 rgba(245,197,24,.4)}50%{transform:scale(1.05);box-shadow:0 0 0 12px rgba(245,197,24,0)}}
.loader-text{font-family:'Bebas Neue';font-size:1.4rem;letter-spacing:4px;color:var(--txt2)}

/* ── ADS ─────────────────────────────────────────────────────── */
.ads-slot{margin:8px 40px;border-radius:var(--radius);overflow:hidden;min-height:1px}

/* ── FOOTER ──────────────────────────────────────────────────── */
footer{background:var(--bg2);border-top:1px solid rgba(255,255,255,.05);padding:40px;margin-top:40px}
.footer-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:32px;margin-bottom:32px}
.footer-col h4{font-size:.8rem;font-weight:800;text-transform:uppercase;letter-spacing:1px;color:var(--gold);margin-bottom:14px}
.footer-col a,.footer-col p{display:block;font-size:.82rem;color:var(--txt2);margin-bottom:8px;transition:color .2s}
.footer-col a:hover{color:var(--txt)}
.footer-bottom{border-top:1px solid rgba(255,255,255,.05);padding-top:20px;display:flex;justify-content:space-between;align-items:center;flex-wrap:gap 16px}
.footer-bottom p{font-size:.78rem;color:var(--txt3)}
.footer-logo{display:flex;align-items:center;gap:8px}

/* ── RESPONSIVE ──────────────────────────────────────────────── */
@media(max-width:768px){
  .hero-content{padding:0 20px;max-width:100%}
  .section{padding:20px 16px}
  .cat-filters{padding:0 16px 16px}
  .ads-slot{margin:8px 16px}
  .hero-dots{left:20px}
  .hero-arrows{display:none}
  .nav-links{display:none}
  #searchBar input{width:150px}
  #searchBar input:focus{width:190px}
  .form-grid{grid-template-columns:1fr}
  .movie-modal-title{font-size:1.8rem}
  .movie-modal-top{height:280px}
  #videoPlayer{margin:0 16px 16px}
  .movie-modal-body,.movie-modal-info{padding-left:16px;padding-right:16px}
  table.data-table{font-size:.75rem}
  footer{padding:24px 16px}
  #searchResults{padding:20px 16px}
}
.hidden{display:none!important}
.empty-state{text-align:center;padding:60px 20px;color:var(--txt2)}
.empty-state .icon{font-size:3rem;margin-bottom:12px;opacity:.4}
.empty-state p{font-size:.88rem}
</style>
</head>
<body>

<!-- PAGE LOADER -->
<div id="pageLoader">
  <div class="loader-logo">
    <div class="logo-k">K</div>
    <div class="loader-text">KEY MOVIES</div>
    <div style="display:flex;justify-content:center;margin-top:20px"><div class="spinner"></div></div>
  </div>
</div>

<!-- TOAST -->
<div id="toast"></div>

<!-- NAVBAR -->
<nav id="navbar">
  <div class="logo" onclick="goHome()">
    <div class="logo-k">K</div>
    <span class="logo-text" id="siteName">KEY MOVIES</span>
  </div>
  <div class="nav-links">
    <a href="#" onclick="filterSection('all')" class="active" id="navHome" data-tr="home">Home</a>
    <a href="#" onclick="filterSection('movie')" id="navMovies" data-tr="movies">Movies</a>
    <a href="#" onclick="filterSection('series')" id="navSeries" data-tr="series">TV Shows</a>
    <a href="#" onclick="showNewReleases()" id="navNew" data-tr="newReleases">New</a>
    <a href="#" onclick="showMyList()" id="navList" data-tr="myList">My List</a>
  </div>
  <div class="nav-right">
    <div id="searchBar">
      <span class="s-icon">🔍</span>
      <input type="text" id="searchInput" placeholder="Search..." oninput="onSearch(this.value)" onkeydown="if(event.key==='Escape')closeSearch()">
    </div>
    <button class="lang-btn" onclick="toggleLang()">🌐 <span id="langLabel">EN</span></button>
    <div id="userMenu">
      <div id="userAvatar" class="user-avatar" onclick="toggleUserMenu()" style="display:none"></div>
      <button class="btn btn-outline btn-sm" id="loginBtn" onclick="showAuth('login')" data-tr="signIn">Sign In</button>
      <button class="btn btn-gold btn-sm" id="signupBtn" onclick="showAuth('register')" data-tr="signUp">Sign Up</button>
      <div class="user-dropdown" id="userDropdown">
        <a href="#" onclick="closeDropdown();showMyList()">📋 <span data-tr="myList">My List</span></a>
        <a href="#" onclick="closeDropdown();showProfile()">👤 <span data-tr="profile">Profile</span></a>
        <button onclick="logout()">🚪 <span data-tr="logout">Logout</span></button>
        <button id="adminDropLink" onclick="closeDropdown();showAdminLogin()" style="display:none" data-tr="adminPanel">⚙️ Admin</button>
      </div>
    </div>
    <button id="adminBtn" class="btn btn-outline btn-sm" onclick="showAdminLogin()" data-tr="admin">Admin</button>
  </div>
</nav>

<!-- SEARCH RESULTS OVERLAY -->
<div id="searchResults">
  <button style="float:right;font-size:1.5rem;color:var(--txt2);margin-bottom:16px" onclick="closeSearch()">✕</button>
  <div class="sr-title" id="srTitle">Results for "<span class="sr-query" id="srQuery"></span>"</div>
  <div class="sr-sub" id="srSub"></div>
  <div class="movies-grid" id="srMovies"></div>
  <div id="srSimilar" style="margin-top:32px"></div>
</div>

<!-- HERO -->
<section id="hero">
  <div class="hero-bg" id="heroBg"></div>
  <div class="hero-content">
    <div class="hero-badge" id="heroBadge">⭐ Featured</div>
    <h1 class="hero-title" id="heroTitle">KEY MOVIES</h1>
    <div class="hero-meta" id="heroMeta"></div>
    <p class="hero-desc" id="heroDesc">Stream and download your favourite movies and TV series. New content added every day.</p>
    <div class="hero-btns">
      <button class="btn-play" id="heroPlayBtn" onclick="heroPlay()">▶ Play Now</button>
      <button class="btn-watch" id="heroInfoBtn" onclick="heroInfo()">ℹ More Info</button>
    </div>
  </div>
  <div class="hero-dots" id="heroDots"></div>
  <div class="hero-arrows">
    <div class="hero-arrow" onclick="heroSlide(-1)">▲</div>
    <div class="hero-arrow" onclick="heroSlide(1)">▼</div>
  </div>
</section>

<!-- ADS SLOT 1 -->
<div class="ads-slot" id="adsSlot1"></div>

<!-- CATEGORY FILTER -->
<div class="cat-filters" id="catFilters">
  <button class="cat-btn active" onclick="setCat('all',this)" data-tr="all">All</button>
  <button class="cat-btn" onclick="setCat('english',this)">🇬🇧 English</button>
  <button class="cat-btn" onclick="setCat('french',this)">🇫🇷 French</button>
  <button class="cat-btn" onclick="setCat('drama',this)" data-tr="drama">Drama</button>
  <button class="cat-btn" onclick="setCat('chinese',this)">🇨🇳 Chinese</button>
  <button class="cat-btn" onclick="setCat('indian',this)">🇮🇳 Indian</button>
  <button class="cat-btn" onclick="setCat('cartoon',this)" data-tr="cartoon">Cartoon</button>
  <button class="cat-btn" onclick="setCat('series',this)" data-tr="series">📺 Series</button>
</div>

<!-- MAIN SECTIONS -->
<main id="main">
  <!-- TRENDING NOW -->
  <section class="section" id="trendingSection">
    <div class="section-header">
      <h2 class="section-title"><span>🔥</span> <span data-tr="trending">Trending Now</span></h2>
      <div class="section-line"></div>
      <div class="section-nav">
        <button onclick="scrollRow('trendingRow',-1)">◀</button>
        <button onclick="scrollRow('trendingRow',1)">▶</button>
      </div>
    </div>
    <div class="movies-row" id="trendingRow"></div>
  </section>

  <!-- NEW RELEASES (logged in only) -->
  <section class="section" id="newSection" style="display:none">
    <div class="section-header">
      <h2 class="section-title"><span>✨</span> <span data-tr="newReleases">Newly Released</span></h2>
      <div class="section-line"></div>
      <div class="section-nav">
        <button onclick="scrollRow('newRow',-1)">◀</button>
        <button onclick="scrollRow('newRow',1)">▶</button>
      </div>
    </div>
    <div class="movies-row" id="newRow"></div>
  </section>

  <!-- ADS SLOT 2 -->
  <div class="ads-slot" id="adsSlot2"></div>

  <!-- ALL MOVIES GRID -->
  <section class="section">
    <div class="section-header">
      <h2 class="section-title" id="gridTitle">All Movies</h2>
      <div class="section-line"></div>
    </div>
    <div class="movies-grid" id="moviesGrid"></div>
    <div class="empty-state hidden" id="emptyState">
      <div class="icon">🎬</div>
      <p id="emptyMsg">No movies found.</p>
    </div>
  </section>

  <!-- LOGGED OUT MESSAGE -->
  <div id="guestBanner" style="background:linear-gradient(135deg,var(--bg2),var(--bg3));border:1px solid rgba(245,197,24,.2);border-radius:16px;margin:0 40px 40px;padding:32px;text-align:center;display:none">
    <div style="font-size:2rem;margin-bottom:12px">🔒</div>
    <h3 style="font-family:'Bebas Neue';font-size:1.6rem;margin-bottom:8px" data-tr="guestTitle">Want to see more?</h3>
    <p style="color:var(--txt2);font-size:.88rem;margin-bottom:20px" data-tr="guestSub">Create a free account to access hundreds of new movies, search and more.</p>
    <button class="btn btn-gold" onclick="showAuth('register')" data-tr="createAccount">Create Free Account</button>
  </div>
</main>

<!-- ADS SLOT 3 -->
<div class="ads-slot" id="adsSlot3"></div>

<!-- FOOTER -->
<footer>
  <div class="footer-grid">
    <div class="footer-col">
      <div class="footer-logo" style="margin-bottom:14px">
        <div class="logo-k" style="width:32px;height:32px;font-size:18px;border-radius:6px">K</div>
        <span id="footerSiteName" style="font-family:'Bebas Neue';font-size:1.1rem;letter-spacing:2px;color:var(--gold)">KEY MOVIES</span>
      </div>
      <p data-tr="footerDesc">The best place to stream and download movies worldwide.</p>
    </div>
    <div class="footer-col">
      <h4 data-tr="browse">Browse</h4>
      <a href="#" onclick="filterSection('all')">Home</a>
      <a href="#" onclick="filterSection('movie')">Movies</a>
      <a href="#" onclick="filterSection('series')">TV Shows</a>
    </div>
    <div class="footer-col">
      <h4 data-tr="community">Community</h4>
      <a href="#" onclick="showAuth('register')" data-tr="joinUs">Join Us</a>
      <a href="#" onclick="showAuth('login')" data-tr="signIn">Sign In</a>
    </div>
    <div class="footer-col">
      <h4 data-tr="helpCenter">Help</h4>
      <a href="#">Contact</a>
      <a href="#">Privacy</a>
      <a href="#">Terms</a>
    </div>
    <div class="footer-col">
      <h4>Follow Us</h4>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <a href="#" style="background:var(--bg3);border-radius:6px;padding:6px 10px">📘</a>
        <a href="#" style="background:var(--bg3);border-radius:6px;padding:6px 10px">🐦</a>
        <a href="#" style="background:var(--bg3);border-radius:6px;padding:6px 10px">📸</a>
        <a href="#" style="background:var(--bg3);border-radius:6px;padding:6px 10px">▶️</a>
      </div>
    </div>
  </div>
  <div class="footer-bottom">
    <p>© <span id="fyear"></span> <span id="footerBrand">KEY MOVIES</span>. All rights reserved.</p>
    <p style="font-size:.72rem;color:var(--txt3)" data-tr="footerCountry">Available worldwide</p>
  </div>
</footer>

<!-- ── AUTH MODAL ─────────────────────────────────────────────── -->
<div class="modal-overlay" id="authModal" onclick="if(event.target===this)closeModal('authModal')">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('authModal')">✕</button>
    <div class="auth-logo">
      <div class="logo-k">K</div>
      <div style="font-family:'Bebas Neue';font-size:1.4rem;color:var(--gold)">KEY MOVIES</div>
    </div>
    <div class="auth-tabs">
      <div class="auth-tab active" id="loginTab" onclick="switchAuthTab('login')">Sign In</div>
      <div class="auth-tab" id="registerTab" onclick="switchAuthTab('register')">Create Account</div>
      <div class="auth-tab" id="adminTab" onclick="switchAuthTab('admin')">Admin</div>
    </div>
    <!-- Login -->
    <div class="auth-body" id="loginBody">
      <div class="form-group">
        <label class="form-label">Gmail Address</label>
        <input type="email" class="form-input" id="loginEmail" placeholder="yourname@gmail.com">
      </div>
      <div class="form-group">
        <label class="form-label">Password</label>
        <input type="password" class="form-input" id="loginPass" placeholder="••••••••" onkeydown="if(event.key==='Enter')doLogin()">
      </div>
      <button class="btn btn-gold" style="width:100%;padding:12px" onclick="doLogin()">Sign In →</button>
      <div class="form-footer">Don't have an account? <a href="#" onclick="switchAuthTab('register')">Create one free</a></div>
    </div>
    <!-- Register -->
    <div class="auth-body hidden" id="registerBody">
      <div class="form-group">
        <label class="form-label">Full Name</label>
        <input type="text" class="form-input" id="regName" placeholder="John Doe">
      </div>
      <div class="form-group">
        <label class="form-label">Gmail Address</label>
        <input type="email" class="form-input" id="regEmail" placeholder="yourname@gmail.com">
      </div>
      <div class="form-group">
        <label class="form-label">Password (min 6 chars)</label>
        <input type="password" class="form-input" id="regPass" placeholder="••••••••">
      </div>
      <div class="form-group">
        <label class="form-label">Country</label>
        <select class="form-select" id="regCountry">
          <option value="">-- Select Country --</option>
          <option>Rwanda</option><option>Nigeria</option><option>Kenya</option><option>Ghana</option>
          <option>South Africa</option><option>Ethiopia</option><option>Tanzania</option>
          <option>Uganda</option><option>Cameroon</option><option>Senegal</option>
          <option>France</option><option>USA</option><option>UK</option>
          <option>Canada</option><option>India</option><option>China</option>
          <option>Other</option>
        </select>
      </div>
      <button class="btn btn-gold" style="width:100%;padding:12px" onclick="doRegister()">Create Account →</button>
      <div class="form-footer">Already have an account? <a href="#" onclick="switchAuthTab('login')">Sign in</a></div>
    </div>
    <!-- Admin -->
    <div class="auth-body hidden" id="adminBody">
      <div style="text-align:center;margin-bottom:20px;font-size:2rem">🔐</div>
      <div class="form-group">
        <label class="form-label">Admin Password</label>
        <input type="password" class="form-input" id="adminPass" placeholder="••••••••" onkeydown="if(event.key==='Enter')doAdminLogin()">
      </div>
      <button class="btn btn-red" style="width:100%;padding:12px" onclick="doAdminLogin()">Enter Admin Panel →</button>
    </div>
  </div>
</div>

<!-- ── MOVIE DETAIL MODAL ───────────────────────────────────────── -->
<div class="modal-overlay" id="movieModal" onclick="if(event.target===this)closeMovieModal()">
  <div class="modal" style="max-width:900px">
    <button class="modal-close" onclick="closeMovieModal()" style="z-index:20">✕</button>
    <div class="movie-modal-top">
      <img class="movie-modal-bg" id="mmBg" src="" alt="">
      <div class="movie-modal-gradient"></div>
      <div class="movie-modal-info">
        <div class="movie-modal-badge" id="mmBadges"></div>
        <h2 class="movie-modal-title" id="mmTitle">Movie Title</h2>
        <div class="movie-modal-meta" id="mmMeta"></div>
      </div>
    </div>
    <div id="videoPlayer"><div id="vpContent"></div></div>
    <div class="movie-modal-body">
      <div class="movie-modal-actions" id="mmActions"></div>
      <p class="movie-modal-desc" id="mmDesc"></p>
      <div class="comments-section">
        <h3>💬 Comments <span id="commentCount" style="color:var(--txt2);font-size:.8rem"></span></h3>
        <div id="commentFormArea">
          <div class="login-to-comment" onclick="showAuth('login')">
            🔒 Sign in to leave a comment
          </div>
        </div>
        <div id="commentsList"></div>
      </div>
    </div>
  </div>
</div>

<!-- ── ADMIN PANEL ─────────────────────────────────────────────── -->
<div id="adminPanel">
  <div class="admin-bar">
    <div class="admin-title">⚙️ ADMIN PANEL</div>
    <div class="admin-nav">
      <button class="active" onclick="adminTab('dashboard')">📊 Dashboard</button>
      <button onclick="adminTab('movies')">🎬 Movies</button>
      <button onclick="adminTab('users')">👥 Users</button>
      <button onclick="adminTab('comments')">💬 Comments</button>
      <button onclick="adminTab('traffic')">📈 Traffic</button>
      <button onclick="adminTab('settings')">⚙️ Settings</button>
    </div>
    <button class="admin-close" onclick="closeAdmin()">✕</button>
  </div>
  <div class="admin-body" id="adminBody2">
    <div style="display:grid;place-items:center;padding:80px"><div class="spinner"></div></div>
  </div>
</div>

<script>
// ── STATE ────────────────────────────────────────────────────────
var STATE = {
  user: null,
  adminToken: null,
  movies: [],
  settings: {},
  currentMovie: null,
  heroMovies: [],
  heroIdx: 0,
  heroTimer: null,
  lang: 'en',
  myList: [],
  activeCat: 'all',
  activeType: 'all',
  searchTimer: null
};

// ── TRANSLATIONS ─────────────────────────────────────────────────
var TR = {
  en: {home:'Home',movies:'Movies',series:'TV Shows',newReleases:'New Releases',myList:'My List',
       signIn:'Sign In',signUp:'Sign Up',profile:'Profile',logout:'Logout',admin:'Admin',
       adminPanel:'Admin Panel',trending:'Trending Now',all:'All',drama:'Drama',cartoon:'Cartoon',
       guestTitle:'Want to see more?',guestSub:'Create a free account to access new movies.',
       createAccount:'Create Free Account',browse:'Browse',community:'Community',helpCenter:'Help',
       footerDesc:'Stream movies worldwide.',footerCountry:'Available worldwide',joinUs:'Join Us'},
  fr: {home:'Accueil',movies:'Films',series:'Séries TV',newReleases:'Nouveautés',myList:'Ma Liste',
       signIn:'Connexion',signUp:"S'inscrire",profile:'Profil',logout:'Déconnexion',admin:'Admin',
       adminPanel:'Panneau Admin',trending:'Tendances',all:'Tout',drama:'Drame',cartoon:'Dessin Animé',
       guestTitle:'Envie de voir plus ?',guestSub:'Créez un compte gratuit pour accéder aux nouveaux films.',
       createAccount:'Créer un Compte',browse:'Parcourir',community:'Communauté',helpCenter:'Aide',
       footerDesc:'Regardez des films partout.',footerCountry:'Disponible dans le monde',joinUs:'Rejoindre'}
};

function t(key){ return (TR[STATE.lang]||TR.en)[key]||key; }

function applyLang(){
  document.querySelectorAll('[data-tr]').forEach(function(el){
    var k=el.getAttribute('data-tr'), v=t(k);
    if(v) el.textContent=v;
  });
}

function toggleLang(){
  STATE.lang = STATE.lang==='en'?'fr':'en';
  document.getElementById('langLabel').textContent=STATE.lang.toUpperCase();
  applyLang();
}

// ── API CALL ─────────────────────────────────────────────────────
function api(action, data, cb, errCb) {
  data = data||{};
  data.action = action;
  google.script.run
    .withSuccessHandler(function(res){ if(cb) cb(res); })
    .withFailureHandler(function(e){ console.error(e); if(errCb) errCb(e); else toast('Server error: '+e.message,'error'); })
    .serverAction(data);
}

// ── TOAST ─────────────────────────────────────────────────────────
function toast(msg, type){
  var t=document.getElementById('toast');
  t.textContent=msg; t.className='show '+(type||'');
  setTimeout(function(){t.className='';},3000);
}

// ── INIT ─────────────────────────────────────────────────────────
window.onload = function(){
  document.getElementById('fyear').textContent=new Date().getFullYear();
  // Restore session
  var saved=sessionStorage.getItem('km_user');
  if(saved){ try{ STATE.user=JSON.parse(saved); }catch(e){} }
  var savedAdmin=sessionStorage.getItem('km_admin');
  if(savedAdmin) STATE.adminToken=savedAdmin;
  // Restore myList
  var savedList=localStorage.getItem('km_mylist');
  if(savedList){ try{ STATE.myList=JSON.parse(savedList); }catch(e){} }

  updateNavUI();

  // Load settings then movies
  api('getSettings',{},function(res){
    if(res.ok){
      STATE.settings=res.settings;
      applySettings();
    }
    loadMovies();
    logVisit();
  });
  applyLang();
  window.addEventListener('scroll',onScroll);
  document.getElementById('searchInput').addEventListener('keydown',function(e){
    if(e.key==='Escape') closeSearch();
  });
};

function logVisit(){
  api('logTraffic',{user:STATE.user?STATE.user.gmail:'guest',action:'visit',country:STATE.user?STATE.user.country:'',details:navigator.language});
}

function applySettings(){
  var s=STATE.settings;
  var sn=s['site_name']||'KEY MOVIES';
  document.title=sn;
  var snEl=document.getElementById('siteName'); if(snEl)snEl.textContent=sn;
  var fn=document.getElementById('footerSiteName'); if(fn)fn.textContent=sn;
  var fb=document.getElementById('footerBrand'); if(fb)fb.textContent=sn;

  // Favicon
  if(s['favicon']){
    var link=document.querySelector("link[rel~='icon']");
    if(!link){link=document.createElement('link');link.rel='icon';document.head.appendChild(link);}
    link.href=s['favicon'];
  }
  // Ads
  if(s['ads_code_1']) document.getElementById('adsSlot1').innerHTML=s['ads_code_1'];
  if(s['ads_code_2']) document.getElementById('adsSlot2').innerHTML=s['ads_code_2'];
  if(s['ads_code_3']) document.getElementById('adsSlot3').innerHTML=s['ads_code_3'];
  // Background
  if(s['background_image']){
    document.body.style.backgroundImage='url('+s['background_image']+')';
    document.body.style.backgroundSize='cover';
    document.body.style.backgroundAttachment='fixed';
    document.body.style.backgroundPosition='center';
  }
}

function onScroll(){
  var nb=document.getElementById('navbar');
  if(window.scrollY>60) nb.classList.add('scrolled');
  else nb.classList.remove('scrolled');
}

// ── LOAD MOVIES ───────────────────────────────────────────────────
function loadMovies(cat,type){
  var params={isLoggedIn:!!STATE.user,category:cat||STATE.activeCat,type:type||STATE.activeType};
  api('getMovies',params,function(res){
    document.getElementById('pageLoader').style.display='none';
    if(!res.ok){toast(res.msg,'error');return;}
    STATE.movies=res.movies||[];
    renderAll();
  });
}

function renderAll(){
  var movies=STATE.movies;
  // Hero
  var featured=movies.filter(function(m){return m.featured;});
  if(!featured.length) featured=movies.slice(0,Math.min(5,movies.length));
  STATE.heroMovies=featured;
  STATE.heroIdx=0;
  buildHeroDots();
  renderHero(0);
  startHeroTimer();
  // Trending (all)
  renderRow('trendingRow',movies.filter(function(m){return !m.isNew;}).slice(0,20));
  // New releases (if logged in)
  if(STATE.user){
    var newMovies=movies.filter(function(m){return m.isNew;});
    document.getElementById('newSection').style.display=newMovies.length?'block':'none';
    renderRow('newRow',newMovies.slice(0,20));
    document.getElementById('guestBanner').style.display='none';
  } else {
    document.getElementById('newSection').style.display='none';
    document.getElementById('guestBanner').style.display='block';
  }
  // Grid
  renderGrid(movies);
}

function renderHero(idx){
  var m=STATE.heroMovies[idx];
  if(!m) return;
  var bg=document.getElementById('heroBg');
  bg.style.backgroundImage='url('+( m.cover||'')+')'  ;
  document.getElementById('heroTitle').textContent=m.name||'KEY MOVIES';
  document.getElementById('heroDesc').textContent=m.description||'Stream and download movies worldwide.';
  var meta=document.getElementById('heroMeta');
  meta.innerHTML='<span class="rating">⭐ '+(m.rating||'N/A')+'</span><span>'+m.year+'</span><span>'+capitalize(m.category||'')+'</span>';
  document.getElementById('heroBadge').textContent=m.isNew?'🆕 New Release':'⭐ Featured';
  document.querySelectorAll('.hero-dot').forEach(function(d,i){d.classList.toggle('active',i===idx);});
}

function buildHeroDots(){
  var c=document.getElementById('heroDots');
  c.innerHTML='';
  STATE.heroMovies.forEach(function(_,i){
    var d=document.createElement('div');
    d.className='hero-dot'+(i===0?' active':'');
    d.onclick=function(){heroGoTo(i);};
    c.appendChild(d);
  });
}

function heroPlay(){ if(STATE.heroMovies[STATE.heroIdx]) openMovieModal(STATE.heroMovies[STATE.heroIdx].id,true); }
function heroInfo(){ if(STATE.heroMovies[STATE.heroIdx]) openMovieModal(STATE.heroMovies[STATE.heroIdx].id,false); }
function heroGoTo(i){ STATE.heroIdx=i; renderHero(i); }
function heroSlide(dir){ STATE.heroIdx=(STATE.heroIdx+dir+STATE.heroMovies.length)%STATE.heroMovies.length; renderHero(STATE.heroIdx); }
function startHeroTimer(){
  if(STATE.heroTimer) clearInterval(STATE.heroTimer);
  STATE.heroTimer=setInterval(function(){ heroSlide(1); },6000);
}

function renderRow(id, movies){
  var c=document.getElementById(id); c.innerHTML='';
  if(!movies.length){ c.innerHTML='<div style="color:var(--txt2);font-size:.85rem;padding:20px">No content available.</div>'; return; }
  movies.forEach(function(m){ c.appendChild(makeCard(m,'row')); });
}

function renderGrid(movies){
  var grid=document.getElementById('moviesGrid');
  var empty=document.getElementById('emptyState');
  grid.innerHTML='';
  if(!movies.length){
    empty.classList.remove('hidden');
    document.getElementById('emptyMsg').textContent=STATE.user?'No movies in this category.':'Sign in to see more content.';
    return;
  }
  empty.classList.add('hidden');
  movies.forEach(function(m){ grid.appendChild(makeCard(m,'grid')); });
}

function makeCard(m, mode){
  var div=document.createElement('div');
  div.className='movie-card';
  if(mode==='grid') div.style.width='100%';
  var inList=STATE.myList.indexOf(m.id)!==-1;
  div.innerHTML='<div class="card-thumb"><img src="'+(m.cover||'data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 width=%22160%22 height=%22240%22><rect fill=%22%231a1a28%22 width=%22160%22 height=%22240%22/><text x=%2280%22 y=%22130%22 text-anchor=%22middle%22 fill=%22%23555%22 font-size=%2240%22>🎬</text></svg>')+'" alt="'+m.name+'" loading="lazy"><div class="card-badges"><span class="badge badge-cat">'+capitalize(m.category||m.language||'')+'</span>'+(m.isNew?'<span class="badge badge-new">NEW</span>':'')+(m.season?'<span class="badge badge-ep">S'+m.season+'</span>':'')+'</div><div class="card-overlay"><div class="play-btn">▶</div></div></div><div class="card-info"><div class="card-title">'+m.name+'</div><div class="card-meta"><span class="card-rating">⭐ '+(m.rating||'?')+'</span><span>'+m.year+'</span></div></div>';
  div.onclick=function(){ openMovieModal(m.id,false); };
  return div;
}

// ── MOVIE MODAL ───────────────────────────────────────────────────
function openMovieModal(id, autoPlay){
  api('getMovie',{id:id},function(res){
    if(!res.ok){toast(res.msg,'error');return;}
    var m=res.movie; STATE.currentMovie=m;
    document.getElementById('mmBg').src=m.cover||'';
    document.getElementById('mmTitle').textContent=m.name;
    document.getElementById('mmBadges').innerHTML='<span class="badge badge-cat">'+capitalize(m.category||'')+'</span>'+(m.isNew?'<span class="badge badge-new">NEW</span>':'')+(m.type==='series'?'<span class="badge badge-ep">Series</span>':'');
    document.getElementById('mmMeta').innerHTML='<span>⭐ '+(m.rating||'N/A')+'</span><span>📅 '+m.year+'</span><span>🌍 '+(m.country||'Int.')+'</span>'+(m.season?'<span>Season '+m.season+'</span>':'')+(m.episode?'<span>Ep. '+m.episode+'</span>':'');
    document.getElementById('mmDesc').textContent=m.description||'';
    // Actions
    var acts=document.getElementById('mmActions'); acts.innerHTML='';
    if(m.videoLink){
      var pb=document.createElement('button');
      pb.className='btn btn-red'; pb.innerHTML='▶ Play';
      pb.onclick=function(){playVideo(m);}; acts.appendChild(pb);
    }
    if(m.downloadLink && STATE.user){
      var db=document.createElement('a');
      db.href=m.downloadLink; db.target='_blank'; db.download='';
      db.className='btn btn-outline'; db.innerHTML='⬇ Download'; acts.appendChild(db);
    } else if(m.downloadLink){
      var db2=document.createElement('button');
      db2.className='btn btn-outline'; db2.innerHTML='⬇ Download (Sign in)'; db2.onclick=function(){showAuth('login');}; acts.appendChild(db2);
    }
    // My list button
    var lb=document.createElement('button');
    var inList=STATE.myList.indexOf(m.id)!==-1;
    lb.className='btn btn-outline'; lb.innerHTML=(inList?'✓ In List':'+ My List');
    lb.onclick=function(){toggleMyList(m.id,lb);}; acts.appendChild(lb);

    // Hide player
    var vp=document.getElementById('videoPlayer');
    vp.style.display='none'; document.getElementById('vpContent').innerHTML='';
    // Load comments
    loadComments(m.id);
    openModal('movieModal');
    // Log traffic
    api('logTraffic',{user:STATE.user?STATE.user.gmail:'guest',action:'view',country:STATE.user?STATE.user.country:'',details:m.name});
    if(autoPlay && m.videoLink) setTimeout(function(){playVideo(m);},500);
  });
}

function closeMovieModal(){
  closeModal('movieModal');
  var vp=document.getElementById('videoPlayer');
  vp.style.display='none'; document.getElementById('vpContent').innerHTML='';
}

function playVideo(m){
  var vp=document.getElementById('videoPlayer');
  var vpC=document.getElementById('vpContent');
  var url=m.videoLink||'';
  vp.style.display='block';
  if(url.includes('youtube.com')||url.includes('youtu.be')){
    var vid=url.includes('youtu.be/')?url.split('youtu.be/')[1].split('?')[0]:new URL(url).searchParams.get('v');
    vpC.innerHTML='<iframe src="https://www.youtube.com/embed/'+vid+'?autoplay=1&rel=0" allowfullscreen allow="autoplay"></iframe>';
  } else if(/\.(mp4|webm|ogg|mkv)(\?|$)/i.test(url)){
    vpC.innerHTML='<video controls autoplay src="'+url+'" style="width:100%;height:100%"></video>';
  } else {
    vpC.innerHTML='<iframe src="'+url+'" allowfullscreen allow="autoplay"></iframe>';
  }
  vp.scrollIntoView({behavior:'smooth',block:'center'});
  api('logTraffic',{user:STATE.user?STATE.user.gmail:'guest',action:'play',country:'',details:(m.name)});
}

// ── COMMENTS ─────────────────────────────────────────────────────
function loadComments(movieId){
  api('getComments',{movieId:movieId},function(res){
    if(!res.ok) return;
    var c=res.comments||[];
    document.getElementById('commentCount').textContent='('+c.length+')';
    var cl=document.getElementById('commentsList'); cl.innerHTML='';
    c.forEach(function(cm){
      var d=document.createElement('div'); d.className='comment-item';
      var initial=(cm.name||cm.gmail||'?')[0].toUpperCase();
      var dt=new Date(cm.date); var dateStr=isNaN(dt)?cm.date:dt.toLocaleDateString();
      d.innerHTML='<div class="comment-avatar">'+initial+'</div><div class="comment-right"><div class="comment-author">'+(cm.name||cm.gmail)+'</div><div class="comment-text">'+escHtml(cm.comment)+'</div><div class="comment-date">'+dateStr+'</div></div>';
      cl.appendChild(d);
    });
    // Comment form
    var cfa=document.getElementById('commentFormArea');
    if(STATE.user){
      cfa.innerHTML='<div class="comment-form"><textarea id="commentText" placeholder="Write a comment..."></textarea><button class="btn btn-gold btn-sm" onclick="postComment()">Post</button></div>';
    } else {
      cfa.innerHTML='<div class="login-to-comment" onclick="closeMovieModal();showAuth(\'login\')">🔒 Sign in to comment</div>';
    }
  });
}

function postComment(){
  var txt=document.getElementById('commentText');
  if(!txt||!txt.value.trim()){toast('Write something first.','error');return;}
  api('addComment',{gmail:STATE.user.gmail,name:STATE.user.name,movieId:STATE.currentMovie.id,comment:txt.value.trim()},function(res){
    if(res.ok){toast('Comment posted! ✓','success');txt.value='';loadComments(STATE.currentMovie.id);}
    else toast(res.msg,'error');
  });
}

// ── MY LIST ──────────────────────────────────────────────────────
function toggleMyList(id,btn){
  var i=STATE.myList.indexOf(id);
  if(i===-1){STATE.myList.push(id);btn.innerHTML='✓ In List';toast('Added to your list ✓','success');}
  else{STATE.myList.splice(i,1);btn.innerHTML='+ My List';toast('Removed from list');}
  localStorage.setItem('km_mylist',JSON.stringify(STATE.myList));
}

function showMyList(){
  if(!STATE.user){showAuth('login');return;}
  var listed=STATE.movies.filter(function(m){return STATE.myList.indexOf(m.id)!==-1;});
  renderGrid(listed);
  document.getElementById('gridTitle').textContent='📋 My List';
  scrollToSection();
}

// ── SEARCH ───────────────────────────────────────────────────────
function onSearch(v){
  clearTimeout(STATE.searchTimer);
  if(!v.trim()){closeSearch();return;}
  STATE.searchTimer=setTimeout(function(){doSearch(v.trim());},400);
}

function doSearch(q){
  api('searchMovies',{query:q,isLoggedIn:!!STATE.user},function(res){
    if(!res.ok) return;
    var sr=document.getElementById('searchResults');
    sr.classList.add('open');
    document.getElementById('srQuery').textContent=q;
    var exact=res.exact||[], similar=res.similar||[];
    document.getElementById('srSub').textContent=exact.length+' exact results, '+similar.length+' similar';
    var m=document.getElementById('srMovies'); m.innerHTML='';
    if(exact.length){
      exact.forEach(function(mv){m.appendChild(makeCard(mv,'grid'));});
    } else {
      m.innerHTML='<div class="no-results-box"><h3>No exact matches for "'+escHtml(q)+'"</h3><p>But we found some similar content below</p></div>';
    }
    var sim=document.getElementById('srSimilar'); sim.innerHTML='';
    if(similar.length){
      sim.innerHTML='<h3 style="font-family:\'Bebas Neue\';font-size:1.2rem;margin-bottom:16px;color:var(--txt2)">Similar Results</h3>';
      var sg=document.createElement('div'); sg.className='movies-grid';
      similar.forEach(function(mv){sg.appendChild(makeCard(mv,'grid'));});
      sim.appendChild(sg);
    }
  });
}

function closeSearch(){
  document.getElementById('searchResults').classList.remove('open');
  document.getElementById('searchInput').value='';
}

// ── CATEGORY FILTER ───────────────────────────────────────────────
function setCat(cat,btn){
  STATE.activeCat=cat;
  document.querySelectorAll('.cat-btn').forEach(function(b){b.classList.remove('active');});
  btn.classList.add('active');
  loadMovies(cat, STATE.activeType==='series'?'series':'all');
  if(cat==='series') STATE.activeType='series';
  else STATE.activeType='all';
  scrollToSection();
}

function filterSection(type){
  STATE.activeType=type;
  if(type==='series') STATE.activeCat='all';
  loadMovies(STATE.activeCat, type);
  document.querySelectorAll('.nav-links a').forEach(function(a){a.classList.remove('active');});
  scrollToSection();
}

function showNewReleases(){
  if(!STATE.user){showAuth('login');return;}
  var newMovies=STATE.movies.filter(function(m){return m.isNew;});
  renderGrid(newMovies);
  document.getElementById('gridTitle').textContent='✨ New Releases';
  scrollToSection();
}

function scrollToSection(){
  document.getElementById('main').scrollIntoView({behavior:'smooth'});
}

function goHome(){
  STATE.activeCat='all';STATE.activeType='all';
  loadMovies('all','all');
  window.scrollTo({top:0,behavior:'smooth'});
}

function scrollRow(id,dir){
  var el=document.getElementById(id);
  if(el) el.scrollBy({left:dir*340,behavior:'smooth'});
}

// ── AUTH ──────────────────────────────────────────────────────────
function showAuth(tab){
  switchAuthTab(tab||'login');
  openModal('authModal');
}

function switchAuthTab(tab){
  ['login','register','admin'].forEach(function(t){
    document.getElementById(t+'Tab').classList.toggle('active',t===tab);
    document.getElementById(t+'Body').classList.toggle('hidden',t!==tab);
  });
}

function doLogin(){
  var e=document.getElementById('loginEmail').value.trim();
  var p=document.getElementById('loginPass').value;
  if(!e||!p){toast('Fill all fields','error');return;}
  api('login',{gmail:e,password:p},function(res){
    if(res.ok){
      STATE.user=res.user;
      sessionStorage.setItem('km_user',JSON.stringify(res.user));
      closeModal('authModal');
      updateNavUI();
      loadMovies();
      toast('Welcome back, '+res.user.name+'! 👋','success');
    } else toast(res.msg,'error');
  });
}

function doRegister(){
  var n=document.getElementById('regName').value.trim();
  var e=document.getElementById('regEmail').value.trim();
  var p=document.getElementById('regPass').value;
  var c=document.getElementById('regCountry').value;
  if(!n||!e||!p){toast('Fill all required fields','error');return;}
  api('register',{name:n,gmail:e,password:p,country:c},function(res){
    if(res.ok){
      STATE.user=res.user;
      sessionStorage.setItem('km_user',JSON.stringify(res.user));
      closeModal('authModal');
      updateNavUI();
      loadMovies();
      toast('Account created! Welcome '+n+' 🎉','success');
    } else toast(res.msg,'error');
  });
}

function doAdminLogin(){
  var p=document.getElementById('adminPass').value;
  api('adminLogin',{password:p},function(res){
    if(res.ok){
      STATE.adminToken=res.token;
      sessionStorage.setItem('km_admin',res.token);
      closeModal('authModal');
      document.getElementById('adminBtn').style.display='none';
      document.getElementById('adminDropLink').style.display='block';
      openAdmin();
      toast('Welcome, Admin ⚙️','success');
    } else toast(res.msg,'error');
  });
}

function logout(){
  STATE.user=null;STATE.adminToken=null;
  sessionStorage.removeItem('km_user');sessionStorage.removeItem('km_admin');
  updateNavUI();loadMovies();
  toast('Logged out. See you soon 👋');
}

function updateNavUI(){
  var u=STATE.user;
  var loginBtn=document.getElementById('loginBtn');
  var signupBtn=document.getElementById('signupBtn');
  var avatarEl=document.getElementById('userAvatar');
  var adminBtn=document.getElementById('adminBtn');
  if(u){
    loginBtn.style.display='none'; signupBtn.style.display='none';
    avatarEl.style.display='grid';
    avatarEl.textContent=(u.name||u.gmail||'?')[0].toUpperCase();
  } else {
    loginBtn.style.display=''; signupBtn.style.display='';
    avatarEl.style.display='none';
  }
  adminBtn.style.display=STATE.adminToken?'none':'';
  document.getElementById('adminDropLink').style.display=STATE.adminToken?'flex':'none';
}

function toggleUserMenu(){
  document.getElementById('userDropdown').classList.toggle('open');
}
function closeDropdown(){ document.getElementById('userDropdown').classList.remove('open'); }
document.addEventListener('click',function(e){
  var um=document.getElementById('userMenu');
  if(um&&!um.contains(e.target)) closeDropdown();
});

function showProfile(){
  if(!STATE.user) return;
  var u=STATE.user;
  toast('Profile: '+u.name+' | '+u.gmail+' | '+u.country);
}

// ── MODAL HELPERS ─────────────────────────────────────────────────
function openModal(id){ document.getElementById(id).classList.add('open'); }
function closeModal(id){ document.getElementById(id).classList.remove('open'); }
document.addEventListener('keydown',function(e){ if(e.key==='Escape'){ closeSearch(); closeModal('authModal'); closeMovieModal(); }});

// ── ADMIN PANEL ───────────────────────────────────────────────────
function showAdminLogin(){ if(STATE.adminToken){ openAdmin(); } else { showAuth('admin'); } }
function openAdmin(){ document.getElementById('adminPanel').classList.add('open'); adminTab('dashboard'); }
function closeAdmin(){ document.getElementById('adminPanel').classList.remove('open'); }

function adminTab(tab){
  document.querySelectorAll('.admin-nav button').forEach(function(b){b.classList.remove('active');});
  var tabs=['dashboard','movies','users','comments','traffic','settings'];
  var idx=tabs.indexOf(tab);
  if(idx!==-1) document.querySelectorAll('.admin-nav button')[idx].classList.add('active');
  var body=document.getElementById('adminBody2');
  body.innerHTML='<div style="display:grid;place-items:center;padding:80px"><div class="spinner"></div></div>';
  switch(tab){
    case 'dashboard': loadAdminDashboard(); break;
    case 'movies':    loadAdminMovies(); break;
    case 'users':     loadAdminUsers(); break;
    case 'comments':  loadAdminComments(); break;
    case 'traffic':   loadAdminTraffic(); break;
    case 'settings':  loadAdminSettings(); break;
  }
}

function loadAdminDashboard(){
  api('getStats',{token:STATE.adminToken},function(res){
    if(!res.ok){document.getElementById('adminBody2').innerHTML='<p style="color:var(--red)">'+res.msg+'</p>';return;}
    var s=res.stats;
    document.getElementById('adminBody2').innerHTML=
      '<div class="stats-grid">'+
      '<div class="stat-card"><div class="stat-icon">👥</div><div class="stat-num">'+s.users+'</div><div class="stat-label">Users</div></div>'+
      '<div class="stat-card"><div class="stat-icon">🎬</div><div class="stat-num">'+s.movies+'</div><div class="stat-label">Movies</div></div>'+
      '<div class="stat-card"><div class="stat-icon">💬</div><div class="stat-num">'+s.comments+'</div><div class="stat-label">Comments</div></div>'+
      '<div class="stat-card"><div class="stat-icon">📈</div><div class="stat-num">'+s.visits+'</div><div class="stat-label">Visits</div></div>'+
      '</div>'+
      '<div style="background:var(--bg3);border-radius:12px;padding:24px;border:1px solid rgba(255,255,255,.06)">'+
      '<h3 style="margin-bottom:16px;font-size:1rem;font-weight:800">📌 Quick Actions</h3>'+
      '<div style="display:flex;gap:10px;flex-wrap:wrap">'+
      '<button class="btn btn-gold btn-sm" onclick="adminTab(\'movies\')">+ Add Movie</button>'+
      '<button class="btn btn-outline btn-sm" onclick="adminTab(\'users\')">Manage Users</button>'+
      '<button class="btn btn-outline btn-sm" onclick="adminTab(\'settings\')">Edit Settings</button>'+
      '<button class="btn btn-outline btn-sm" onclick="adminTab(\'traffic\')">View Traffic</button>'+
      '</div></div>';
  });
}

var editingMovieId=null;
function loadAdminMovies(){
  api('getMovies',{isLoggedIn:true,category:'all',type:'all'},function(res){
    if(!res.ok) return;
    var movies=res.movies||[];
    var rows=movies.map(function(m){
      return '<tr>'+
        '<td><img src="'+(m.cover||'')+ '" style="width:40px;height:56px;object-fit:cover;border-radius:4px;background:var(--bg)"></td>'+
        '<td><strong>'+escHtml(m.name)+'</strong></td>'+
        '<td>'+capitalize(m.type||'')+'</td>'+
        '<td>'+capitalize(m.category||'')+'</td>'+
        '<td>'+m.year+'</td>'+
        '<td><span class="'+(m.isNew?'status-active':'')+'">'+( m.isNew?'New':'Old')+'</span></td>'+
        '<td style="display:flex;gap:6px;padding:10px 14px">'+
        '<button class="btn btn-outline btn-sm" onclick="editMovie(\''+m.id+'\')">✏️</button>'+
        '<button class="btn btn-sm" style="background:rgba(229,9,20,.15);color:var(--red)" onclick="confirmDeleteMovie(\''+m.id+'\',\''+escHtml(m.name)+'\')">🗑</button>'+
        '</td></tr>';
    }).join('');
    document.getElementById('adminBody2').innerHTML=
      '<div class="movie-form" id="movieFormWrap">'+movieFormHTML(null)+'</div>'+
      '<div class="admin-table-wrap">'+
      '<div class="admin-table-head"><h3>🎬 Movies ('+movies.length+')</h3></div>'+
      '<div style="overflow-x:auto"><table class="data-table"><thead><tr><th>Cover</th><th>Name</th><th>Type</th><th>Category</th><th>Year</th><th>Status</th><th>Actions</th></tr></thead><tbody>'+rows+'</tbody></table></div></div>';
  });
}

function movieFormHTML(m){
  var title=m?'Edit: '+m.name:'Add New Movie';
  return '<h3 style="margin-bottom:20px;font-size:1rem;font-weight:800">🎬 '+title+'</h3>'+
    '<div class="form-grid">'+
    '<div class="form-group"><label class="form-label">Movie Name *</label><input class="form-input" id="mfName" value="'+(m?escHtml(m.name):'')+'" placeholder="e.g. Avengers"></div>'+
    '<div class="form-group"><label class="form-label">Year</label><input class="form-input" id="mfYear" type="number" value="'+(m?m.year:new Date().getFullYear())+'" placeholder="2024"></div>'+
    '<div class="form-group"><label class="form-label">Type</label><select class="form-select" id="mfType"><option value="movie" '+(m&&m.type==='movie'?'selected':'')+'>Movie</option><option value="series" '+(m&&m.type==='series'?'selected':'')+'>Series</option></select></div>'+
    '<div class="form-group"><label class="form-label">Category / Language</label><select class="form-select" id="mfCat">'+
    ['english','french','drama','chinese','indian','cartoon'].map(function(c){return '<option value="'+c+'" '+(m&&m.category===c?'selected':'')+'>'+capitalize(c)+'</option>';}).join('')+
    '</select></div>'+
    '<div class="form-group full"><label class="form-label">Cover Image URL</label><input class="form-input" id="mfCover" value="'+(m?m.cover:'')+'" placeholder="https://..."></div>'+
    '<div class="form-group full"><label class="form-label">Video Link (YouTube, MP4, etc.)</label><input class="form-input" id="mfVideo" value="'+(m?m.videoLink:'')+'" placeholder="https://youtube.com/watch?v=..."></div>'+
    '<div class="form-group full"><label class="form-label">Download Link</label><input class="form-input" id="mfDownload" value="'+(m?m.downloadLink:'')+'" placeholder="https://..."></div>'+
    '<div class="form-group full"><label class="form-label">Description</label><textarea class="form-input" id="mfDesc" rows="3" placeholder="Movie synopsis...">'+(m?escHtml(m.description||''):'')+'</textarea></div>'+
    '<div class="form-group"><label class="form-label">Country</label><input class="form-input" id="mfCountry" value="'+(m?m.country:'')+'" placeholder="USA, France..."></div>'+
    '<div class="form-group"><label class="form-label">Rating (e.g. 7.5)</label><input class="form-input" id="mfRating" value="'+(m?m.rating:'')+'" placeholder="7.5"></div>'+
    '<div class="form-group"><label class="form-label">Season (series)</label><input class="form-input" id="mfSeason" value="'+(m?m.season:'')+'" placeholder="1"></div>'+
    '<div class="form-group"><label class="form-label">Episode</label><input class="form-input" id="mfEpisode" value="'+(m?m.episode:'')+'" placeholder="1"></div>'+
    '<div class="form-group"><label class="form-label" style="display:flex;align-items:center;gap:8px"><input type="checkbox" id="mfIsNew" '+(m&&m.isNew?'checked':'')+'> Mark as New Release</label></div>'+
    '<div class="form-group"><label class="form-label" style="display:flex;align-items:center;gap:8px"><input type="checkbox" id="mfFeatured" '+(m&&m.featured?'checked':'')+'> Feature on Hero Banner</label></div>'+
    '</div>'+
    '<div style="display:flex;gap:10px;margin-top:8px">'+
    '<button class="btn btn-gold" onclick="saveMovie()">'+(m?'💾 Update Movie':'➕ Add Movie')+'</button>'+
    (m?'<button class="btn btn-outline" onclick="cancelEditMovie()">Cancel</button>':'')+
    '</div>';
}

function editMovie(id){
  api('getMovie',{id:id},function(res){
    if(!res.ok) return;
    editingMovieId=id;
    document.getElementById('movieFormWrap').innerHTML=movieFormHTML(res.movie);
    document.getElementById('movieFormWrap').scrollIntoView({behavior:'smooth'});
  });
}

function cancelEditMovie(){ editingMovieId=null; document.getElementById('movieFormWrap').innerHTML=movieFormHTML(null); }

function saveMovie(){
  var d={
    token:STATE.adminToken,
    name:document.getElementById('mfName').value.trim(),
    year:document.getElementById('mfYear').value,
    type:document.getElementById('mfType').value,
    category:document.getElementById('mfCat').value,
    cover:document.getElementById('mfCover').value.trim(),
    videoLink:document.getElementById('mfVideo').value.trim(),
    downloadLink:document.getElementById('mfDownload').value.trim(),
    description:document.getElementById('mfDesc').value.trim(),
    country:document.getElementById('mfCountry').value.trim(),
    rating:document.getElementById('mfRating').value.trim(),
    season:document.getElementById('mfSeason').value.trim(),
    episode:document.getElementById('mfEpisode').value.trim(),
    isNew:document.getElementById('mfIsNew').checked,
    featured:document.getElementById('mfFeatured').checked
  };
  if(!d.name){toast('Movie name required','error');return;}
  if(editingMovieId){
    d.id=editingMovieId;
    api('updateMovie',d,function(res){
      if(res.ok){toast(res.msg,'success');editingMovieId=null;loadAdminMovies();loadMovies();}
      else toast(res.msg,'error');
    });
  } else {
    api('addMovie',d,function(res){
      if(res.ok){toast(res.msg,'success');loadAdminMovies();loadMovies();}
      else toast(res.msg,'error');
    });
  }
}

function confirmDeleteMovie(id,name){
  if(confirm('Delete "'+name+'"? This cannot be undone.')){
    api('deleteMovie',{token:STATE.adminToken,id:id},function(res){
      if(res.ok){toast('Movie deleted','success');loadAdminMovies();loadMovies();}
      else toast(res.msg,'error');
    });
  }
}

function loadAdminUsers(){
  api('getUsers',{token:STATE.adminToken},function(res){
    if(!res.ok) return;
    var users=res.users||[];
    var rows=users.map(function(u){
      var status=u.status||'active';
      return '<tr>'+
        '<td><strong>'+escHtml(u.name||'')+'</strong></td>'+
        '<td style="color:var(--gold)">'+escHtml(u.gmail)+'</td>'+
        '<td>'+(u.country||'-')+'</td>'+
        '<td>'+((u.comments||[]).length)+' comments</td>'+
        '<td><span class="status-'+status+'">'+status+'</span></td>'+
        '<td style="display:flex;gap:6px;padding:10px 14px">'+
        '<button class="btn btn-outline btn-sm" onclick="viewUserComments(\''+u.gmail+'\',\''+escHtml(JSON.stringify(u.comments||[]).replace(/'/g,"\\'")+'\')">💬</button>'+
        (status==='active'?
          '<button class="btn btn-sm" style="background:rgba(229,9,20,.15);color:var(--red)" onclick="setUserStatus(\''+u.id+'\',\'blocked\')">🔒 Block</button>':
          '<button class="btn btn-sm" style="background:rgba(39,174,96,.15);color:#27ae60" onclick="setUserStatus(\''+u.id+'\',\'active\')">✅ Unblock</button>')+
        '<button class="btn btn-sm" style="background:rgba(229,9,20,.1);color:var(--red)" onclick="confirmDeleteUser(\''+u.id+'\',\''+escHtml(u.name)+'\')">🗑</button>'+
        '</td></tr>';
    }).join('');
    document.getElementById('adminBody2').innerHTML=
      '<div class="admin-table-wrap">'+
      '<div class="admin-table-head"><h3>👥 Users ('+users.length+')</h3></div>'+
      '<div style="overflow-x:auto"><table class="data-table"><thead><tr><th>Name</th><th>Gmail</th><th>Country</th><th>Activity</th><th>Status</th><th>Actions</th></tr></thead><tbody>'+rows+'</tbody></table></div></div>'+
      '<div id="userCommentsPanel" style="margin-top:24px"></div>';
  });
}

function setUserStatus(id,status){
  api('updateUserStatus',{token:STATE.adminToken,id:id,status:status},function(res){
    if(res.ok){toast('Status updated to: '+status,'success');loadAdminUsers();}
    else toast(res.msg,'error');
  });
}

function confirmDeleteUser(id,name){
  if(confirm('Delete user "'+name+'"?')){
    api('deleteUser',{token:STATE.adminToken,id:id},function(res){
      if(res.ok){toast('User deleted','success');loadAdminUsers();}
      else toast(res.msg,'error');
    });
  }
}

function viewUserComments(gmail,comments){
  var c;try{c=JSON.parse(comments||'[]');}catch(e){c=[];}
  var panel=document.getElementById('userCommentsPanel');
  panel.innerHTML='<div class="admin-table-wrap"><div class="admin-table-head"><h3>💬 Comments by '+gmail+'</h3><button class="btn btn-outline btn-sm" onclick="document.getElementById(\'userCommentsPanel\').innerHTML=\'\'">Close</button></div>'+
    (c.length?c.map(function(cm){return '<div style="padding:12px 16px;border-bottom:1px solid rgba(255,255,255,.04)"><div style="font-size:.72rem;color:var(--txt2)">Movie ID: '+cm.movieId+' | '+new Date(cm.date).toLocaleDateString()+'</div><div style="font-size:.85rem;margin-top:4px">'+escHtml(cm.comment)+'</div></div>';}).join(''):
    '<div style="padding:20px;text-align:center;color:var(--txt2)">No comments</div>')+
    '</div>';
  panel.scrollIntoView({behavior:'smooth'});
}

function loadAdminComments(){
  api('getAllComments',{token:STATE.adminToken},function(res){
    if(!res.ok) return;
    var comments=res.comments||[];
    var rows=comments.map(function(c){
      var dt=new Date(c.date);
      return '<tr>'+
        '<td style="color:var(--gold)">'+escHtml(c.name||c.gmail)+'</td>'+
        '<td style="color:var(--txt2);font-size:.75rem">'+c.movieId+'</td>'+
        '<td>'+escHtml(c.comment.substring(0,80))+(c.comment.length>80?'...':'')+'</td>'+
        '<td style="color:var(--txt3);font-size:.75rem">'+(!isNaN(dt)?dt.toLocaleDateString():c.date)+'</td>'+
        '<td><button class="btn btn-sm" style="background:rgba(229,9,20,.15);color:var(--red)" onclick="deleteComment(\''+c.id+'\')">🗑 Delete</button></td>'+
        '</tr>';
    }).join('');
    document.getElementById('adminBody2').innerHTML=
      '<div class="admin-table-wrap">'+
      '<div class="admin-table-head"><h3>💬 Comments ('+comments.length+')</h3></div>'+
      '<div style="overflow-x:auto"><table class="data-table"><thead><tr><th>User</th><th>Movie</th><th>Comment</th><th>Date</th><th>Action</th></tr></thead><tbody>'+rows+'</tbody></table></div></div>';
  });
}

function deleteComment(id){
  if(!confirm('Delete this comment?')) return;
  api('deleteComment',{token:STATE.adminToken,id:id},function(res){
    if(res.ok){toast('Deleted','success');loadAdminComments();}
    else toast(res.msg,'error');
  });
}

function loadAdminTraffic(){
  api('getTraffic',{token:STATE.adminToken},function(res){
    if(!res.ok) return;
    var traffic=res.traffic||[];
    var items=traffic.map(function(t){
      var dt=new Date(t.timestamp);
      return '<div class="traffic-item">'+
        '<span class="traffic-time">'+(isNaN(dt)?t.timestamp:dt.toLocaleString())+'</span>'+
        '<span class="traffic-user">'+escHtml(t.user||'guest')+'</span>'+
        '<span class="traffic-action">'+escHtml(t.action||'visit')+'</span>'+
        '<span class="traffic-country">'+escHtml(t.country||'-')+'</span>'+
        '<span style="color:var(--txt3);font-size:.75rem">'+escHtml(t.details||'')+'</span>'+
        '</div>';
    }).join('');
    document.getElementById('adminBody2').innerHTML=
      '<div class="admin-table-wrap">'+
      '<div class="admin-table-head"><h3>📈 Traffic Log ('+traffic.length+' recent)</h3></div>'+
      '<div style="padding:0 20px 20px">'+(items||'<div style="padding:20px;text-align:center;color:var(--txt2)">No traffic yet.</div>')+'</div></div>';
  });
}

function loadAdminSettings(){
  api('getSettings',{},function(res){
    var s=res.settings||{};
    document.getElementById('adminBody2').innerHTML=
      '<div class="movie-form">'+
      '<h3 style="margin-bottom:20px;font-size:1rem;font-weight:800">⚙️ Site Settings</h3>'+
      '<div class="form-grid">'+
      '<div class="form-group"><label class="form-label">Site Name</label><input class="form-input" id="ss_site_name" value="'+escHtml(s['site_name']||'KEY MOVIES')+'"></div>'+
      '<div class="form-group"><label class="form-label">Admin Password</label><input class="form-input" type="password" id="ss_admin_password" placeholder="New password (leave blank to keep)"></div>'+
      '<div class="form-group full"><label class="form-label">Favicon URL</label><input class="form-input" id="ss_favicon" value="'+escHtml(s['favicon']||'')+'" placeholder="https://...icon.png"></div>'+
      '<div class="form-group full"><label class="form-label">Hero Background Image URL</label><input class="form-input" id="ss_background_image" value="'+escHtml(s['background_image']||'')+'" placeholder="https://...background.jpg"></div>'+
      '<div class="form-group full"><label class="form-label">Ads Code 1 (Top Banner)</label><textarea class="form-input" id="ss_ads_code_1" rows="3" placeholder="Paste ad code here...">'+escHtml(s['ads_code_1']||'')+'</textarea></div>'+
      '<div class="form-group full"><label class="form-label">Ads Code 2 (Middle Banner)</label><textarea class="form-input" id="ss_ads_code_2" rows="3" placeholder="Paste ad code here...">'+escHtml(s['ads_code_2']||'')+'</textarea></div>'+
      '<div class="form-group full"><label class="form-label">Ads Code 3 (Bottom Banner)</label><textarea class="form-input" id="ss_ads_code_3" rows="3" placeholder="Paste ad code here...">'+escHtml(s['ads_code_3']||'')+'</textarea></div>'+
      '</div>'+
      '<button class="btn btn-gold" onclick="saveSettings()" style="margin-top:8px">💾 Save All Settings</button>'+
      '</div>';
  });
}

function saveSettings(){
  var settings={
    'site_name':document.getElementById('ss_site_name').value.trim(),
    'favicon':document.getElementById('ss_favicon').value.trim(),
    'background_image':document.getElementById('ss_background_image').value.trim(),
    'ads_code_1':document.getElementById('ss_ads_code_1').value,
    'ads_code_2':document.getElementById('ss_ads_code_2').value,
    'ads_code_3':document.getElementById('ss_ads_code_3').value
  };
  var newPass=document.getElementById('ss_admin_password').value;
  if(newPass) settings['admin_password']=newPass;
  api('updateSettings',{token:STATE.adminToken,settings:settings},function(res){
    if(res.ok){
      toast(res.msg,'success');
      api('getSettings',{},function(r){if(r.ok){STATE.settings=r.settings;applySettings();}});
    } else toast(res.msg,'error');
  });
}

// ── UTILS ──────────────────────────────────────────────────────────
function capitalize(s){ return s?s.charAt(0).toUpperCase()+s.slice(1):'';}
function escHtml(s){ return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
</script>
</body>
</html>
